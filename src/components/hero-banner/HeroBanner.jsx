import React from 'react';

const HeroBanner = ({image}) => {
    return (
        <div className={"mb-[77px]"}>
            <img src={image} alt=""/>
        </div>
    );
};

export default HeroBanner;
